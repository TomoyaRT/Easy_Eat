export default {
    data() {
        return {
            isLoading: false, // Loading元件(全域)
            // 自定義Loading樣式
            loadingObj: {
                bgc: "#FFFFFF",
                style: "dots",
                color: "#FF961F",
                height: 128,
                width: 128,
                opacity: 0.8,
            },
        }
    }
}
